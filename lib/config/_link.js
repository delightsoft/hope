"use strict";

function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

// Generated by CoffeeScript 2.5.1
(function () {
  var $$accessBuilder, $$applyUpdateBuilder, $$editValidateBuilderBuilder, $$getUpdateBuilder, $$newBuilder, $$validateBuilder, BitArray, Result, addValidate, calc, hasOwnProperty, isResult, link, snakeCase, validateStructure;
  Result = require('../result');
  BitArray = require('../bitArray');
  calc = require('../tags/_calc');
  hasOwnProperty = Object.prototype.hasOwnProperty;

  var _require = require('../utils/_err');

  isResult = _require.isResult;
  $$newBuilder = require('./helpers/new');

  var _require2 = require('../validate');

  validateStructure = _require2.structure;
  addValidate = _require2.addValidate;
  $$accessBuilder = require('./helpers/access');
  $$validateBuilder = require('./helpers/validate');
  $$editValidateBuilderBuilder = require('./helpers/editValidateBuilder');
  $$getUpdateBuilder = require('./helpers/getUpdate');
  $$applyUpdateBuilder = require('./helpers/applyUpdate');
  snakeCase = require('lodash/snakeCase');

  link = function link(config, noHelpers, opts) {
    var EMPTY_FLAT_MAP, EMPTY_LIST, EMPTY_MAP, EMPTY_MAP_WITH_TAGS, EMPTY_MASK, EMPTY_TAGS, api, apiKey, _deepFreeze, doc, docKey, freeze, j, l, len, len1, len2, len3, len4, linkFields, linkFieldsWithHelpers, linkFlatMap, linkSortedMap, linkTags, m, method, methods, n, o, ref, ref1, ref10, ref11, ref12, ref13, ref14, ref15, ref16, ref17, ref18, ref19, ref2, ref20, ref21, ref22, ref23, ref24, ref25, ref3, ref4, ref5, ref6, ref7, ref8, ref9, state, transition;

    if (_typeof(noHelpers) === 'object' && noHelpers !== null) {
      methods = noHelpers;
      noHelpers = false;
    }

    freeze = function freeze(obj) {
      if (noHelpers) {
        return obj;
      }

      if (obj.hasOwnProperty('_mask')) {
        obj.list; // force mask to compute list
      }

      return Object.freeze(obj);
    };

    _deepFreeze = function deepFreeze(obj) {
      var fld, fldName, results;
      freeze(obj);
      results = [];

      for (fldName in obj) {
        fld = obj[fldName];

        if (_typeof(fld) === 'object' && fld !== null) {
          results.push(_deepFreeze(fld));
        }
      }

      return results;
    };

    EMPTY_LIST = freeze([]);
    EMPTY_MAP_WITH_TAGS = freeze({
      $$list: EMPTY_LIST,
      $$tags: EMPTY_TAGS
    });
    EMPTY_MAP = freeze({
      $$list: EMPTY_LIST
    });
    EMPTY_MASK = freeze(new BitArray(EMPTY_MAP));
    EMPTY_TAGS = freeze({
      all: EMPTY_MASK
    });
    EMPTY_FLAT_MAP = freeze({
      $$list: EMPTY_LIST,
      $$flat: freeze({
        $$list: EMPTY_LIST
      }, {
        $$tags: EMPTY_TAGS
      })
    });
    EMPTY_MAP_WITH_TAGS = Object.freeze({
      $$list: [],
      $$tags: EMPTY_TAGS
    });

    linkSortedMap = function linkSortedMap(collection, noIndex, noFreeze) {
      var i, j, len, ref, res, v;

      if (collection === void 0) {
        if (noIndex) {
          return EMPTY_MAP;
        } else {
          return EMPTY_MAP_WITH_TAGS;
        }
      }

      res = {};
      ref = collection.list;

      for (i = j = 0, len = ref.length; j < len; i = ++j) {
        v = ref[i];

        if (!noIndex) {
          v.$$index = i;
        }

        res[v.name] = noFreeze ? v : freeze(v);

        if (v.hasOwnProperty('extra')) {
          _deepFreeze(v.extra);
        }
      }

      res.$$list = collection.list;

      if (collection.tags) {
        linkTags(res, collection);
      }

      if (noFreeze) {
        return res;
      } else {
        return freeze(res); // linkSortedMap =
      }
    };

    linkFlatMap = function linkFlatMap(collection, prop, noIndex, noMask, noFreeze) {
      var _linkLevel2, _buildMask, list, map, masks, res;

      if (collection === void 0) {
        return EMPTY_FLAT_MAP;
      }

      map = {};
      list = [];

      _linkLevel2 = function _linkLevel(level) {
        var prefix = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';
        var j, len, v;

        for (j = 0, len = level.length; j < len; j++) {
          v = level[j];

          if (!noIndex) {
            v.$$index = list.length;
          }

          list.push(v);

          if (v.hasOwnProperty(prop)) {
            v[prop] = _linkLevel2(v[prop], "".concat(prefix).concat(v.name, "."));
          }

          map["".concat(prefix).concat(v.name)] = v;
        }

        return linkSortedMap({
          list: level
        }, true, prefix === '' || noFreeze); // _linkLevel
      };

      res = _linkLevel2(collection.list);
      (res.$$flat = map).$$list = freeze(list);
      freeze(map);

      if (collection.tags) {
        linkTags(res, collection);
      }

      if (!noMask) {
        masks = [];

        _buildMask = function buildMask(list) {
          var item, j, l, len, len1, mask, v;

          for (j = 0, len = list.length; j < len; j++) {
            item = list[j];

            for (l = 0, len1 = masks.length; l < len1; l++) {
              v = masks[l];
              v.set(item.$$index);
            }

            if (item.hasOwnProperty(prop)) {
              masks.push(item.$$mask = mask = new BitArray(res));

              _buildMask(item[prop].$$list);

              masks.pop(); // buildMask =
            }
          }
        };

        _buildMask(res.$$list);
      }

      if (noFreeze) {
        return res;
      } else {
        return freeze(res); // linkFlatMode =
      }
    };

    linkTags = function linkTags(res, collection) {
      var cache, k, noCache, ref, ref1, tags, v;
      tags = {
        all: freeze(new BitArray(res).invert())
      };
      ref = collection.tags;

      for (k in ref) {
        v = ref[k];
        tags[k] = freeze(new BitArray(((ref1 = res.$$flat) != null ? ref1.$$list : void 0) || res.$$list, v));
      }

      tags = res.$$tags = freeze(tags);

      if (!noHelpers) {
        cache = Object.create(null);

        noCache = function noCache(result, expr) {
          var localResult, r;

          if (!(_typeof(result) === 'object' && result !== null && result.hasOwnProperty('isError'))) {
            localResult = true;
            result = new Result();
            expr = result;
          }

          r = calc(result, res, expr);

          if (localResult) {
            result.throwIfError();
          }

          return r; // noCache =
        };

        res.$$calc = function (result, expr) {
          if (!(_typeof(result) === 'object' && result !== null && result.hasOwnProperty('isError'))) {
            expr = result;
            result = void 0;
          }

          if (hasOwnProperty.call(cache, expr)) {
            return cache[expr];
          }

          if (!result) {
            result = new Result();
          }

          return cache[expr] = noCache(result, expr); // res.$$calc =
        };

        res.$$calc.noCache = noCache; // linkTags =
      }
    };

    linkFieldsWithHelpers = function linkFieldsWithHelpers(obj, prop, prefix, isDoc) {
      var _assignKey, field, j, l, len, len1, ref, ref1;

      obj[prop] = linkFlatMap(obj[prop], 'fields', false, false, true);
      linkFields(config, obj[prop].$$flat.$$list);

      if (!noHelpers) {
        _assignKey = function assignKey(fields, levelPrefix) {
          var field, j, len, nextLevelPrefix, ref, results;
          ref = fields.$$list;
          results = [];

          for (j = 0, len = ref.length; j < len; j++) {
            field = ref[j];
            field.$$key = nextLevelPrefix = "".concat(levelPrefix, ".field.").concat(field.name);

            if (field.hasOwnProperty('udType')) {
              nextLevelPrefix = "type.".concat(field.udType[field.udType.length - 1]);
            }

            if (field.hasOwnProperty('fields')) {
              _assignKey(field.fields, nextLevelPrefix);
            }

            if (field.hasOwnProperty('enum')) {
              results.push(field["enum"].$$list.forEach(function (e) {
                e.$$key = "".concat(nextLevelPrefix, ".enum.").concat(e.name);
              }));
            } else {
              results.push(void 0);
            }
          }

          return results;
        };

        _assignKey(obj[prop], prefix);

        addValidate(obj[prop], methods != null ? methods.validators : void 0);
        ref = obj[prop].$$flat.$$list;

        for (j = 0, len = ref.length; j < len; j++) {
          field = ref[j];

          if (field.hasOwnProperty('fields')) {
            field.fields.$$new = $$newBuilder(field.fields);
          }
        }

        obj[prop].$$new = $$newBuilder(obj[prop]);
      }

      ref1 = obj[prop].$$flat.$$list;

      for (l = 0, len1 = ref1.length; l < len1; l++) {
        field = ref1[l];

        if ((opts != null ? opts.server : void 0) && isDoc) {
          field.$$field = snakeCase(field.name);
        }

        if (field.hasOwnProperty('enum')) {
          field["enum"].$$list.forEach(function (i) {
            freeze(i);
          });
        }

        if (field.hasOwnProperty('fields')) {
          freeze(field.fields);
        }

        freeze(field); // linkFieldsWithHelpers =
      }
    };

    linkFields = function linkFields(config, list) {
      var field, i, j, len, refName;

      for (j = 0, len = list.length; j < len; j++) {
        field = list[j];

        if (field.hasOwnProperty('udType')) {
          freeze(field.udType);
        }

        if (field.hasOwnProperty('refers')) {
          field.refers = function () {
            var l, len1, ref, results;
            ref = field.refers;
            results = [];

            for (l = 0, len1 = ref.length; l < len1; l++) {
              refName = ref[l];
              results.push(config.docs[refName]);
            }

            return results;
          }();
        }

        if (field.hasOwnProperty('enum')) {
          field["enum"] = linkSortedMap(field["enum"], true, true);
          freeze(field["enum"]);
          freeze(field["enum"].$$list);
        }

        if (field.hasOwnProperty('regexp')) {
          i = regexp.lastIndexOf('/');
          field.regexp = new RegExp(field.regexp.substr(0, i - 1), field.regexp.substr(i + 1)); // linkFields =
        }
      }
    };

    config.docs = linkSortedMap(config.docs, true, true);
    freeze(config.docs);
    freeze(config.docs.$$list);
    ref = config.docs.$$list;

    for (j = 0, len = ref.length; j < len; j++) {
      doc = ref[j];

      if (!noHelpers) {
        doc.$$key = docKey = doc.name;
      }

      if (opts && opts.server) {
        doc.$$table = snakeCase(doc.name);
      }

      linkFieldsWithHelpers(doc, 'fields', docKey, true);
      doc.actions = linkSortedMap(doc.actions, false, true);
      doc.states = linkSortedMap(doc.states, true, true);

      if (!noHelpers) {
        doc.actions.$$list.forEach(function (action) {
          action.$$key = "".concat(docKey, ".action.").concat(action.name);
          freeze(action);
        });
        doc.$$access = $$accessBuilder(doc, 'fields', methods != null ? (ref1 = methods.docs[doc.name]) != null ? ref1.access : void 0 : void 0, true);
        doc.$$validate = $$validateBuilder(doc, 'fields', doc.$$access, methods != null ? (ref2 = methods.docs[doc.name]) != null ? ref2.validate : void 0 : void 0);
        doc.$$editValidateBuilder = $$editValidateBuilderBuilder(doc, 'fields', doc.$$access, methods != null ? (ref3 = methods.docs[doc.name]) != null ? ref3.validate : void 0 : void 0);
        doc.$$getUpdate = $$getUpdateBuilder(doc);
        doc.$$applyUpdate = $$applyUpdateBuilder(doc);
      }

      ref4 = doc.states.$$list;

      for (l = 0, len1 = ref4.length; l < len1; l++) {
        state = ref4[l];

        if (!noHelpers) {
          state.$$key = "".concat(docKey, ".state.").concat(state.name);
        }

        state.view = freeze(new BitArray(doc.fields.$$flat.$$list, state.view));
        state.update = freeze(new BitArray(doc.fields.$$flat.$$list, state.update));
        state.transitions = linkSortedMap(state.transitions, true, false);
        ref5 = state.transitions.$$list;

        for (m = 0, len2 = ref5.length; m < len2; m++) {
          transition = ref5[m];
          transition.next = doc.states[transition.next];
          freeze(transition);
        }

        freeze(state);
      }

      freeze(doc);
    }

    config.api = linkSortedMap(config.api, true, true);
    freeze(config.api);
    freeze(config.api.$$list);
    ref6 = config.api.$$list;

    for (n = 0, len3 = ref6.length; n < len3; n++) {
      api = ref6[n];

      if (!noHelpers) {
        api.$$key = apiKey = "api.".concat(api.name);
      }

      api.methods = linkSortedMap(api.methods, false, true);
      freeze(api.methods);
      freeze(api.methods.$$list);
      ref7 = api.methods.$$list;

      for (o = 0, len4 = ref7.length; o < len4; o++) {
        method = ref7[o];

        if (!noHelpers) {
          method.$$key = "".concat(apiKey, ".method.").concat(method.name);
        }

        linkFieldsWithHelpers(method, 'arguments', "".concat(apiKey, ".method.").concat(method.name, ".arg"));
        linkFieldsWithHelpers(method, 'result', "".concat(apiKey, ".method.").concat(method.name, ".result"));

        if (!noHelpers) {
          method.arguments.$$access = $$accessBuilder(method, 'arguments', methods != null ? (ref8 = methods.api) != null ? (ref9 = ref8[api.name]) != null ? (ref10 = ref9[method.name]) != null ? ref10.argAccess : void 0 : void 0 : void 0 : void 0);
          method.arguments.$$validate = $$validateBuilder(method, 'arguments', method.arguments.$$access, methods != null ? (ref11 = methods.api) != null ? (ref12 = ref11[api.name]) != null ? (ref13 = ref12[method.name]) != null ? ref13.argValidE : void 0 : void 0 : void 0 : void 0);
          method.arguments.$$editValidateBuilder = $$editValidateBuilderBuilder(method, 'arguments', method.arguments.$$access, methods != null ? (ref14 = methods.api) != null ? (ref15 = ref14[api.name]) != null ? (ref16 = ref15[method.name]) != null ? ref16.argValidate : void 0 : void 0 : void 0 : void 0);
          method.result.$$access = $$accessBuilder(method, 'result', methods != null ? (ref17 = methods.api) != null ? (ref18 = ref17[api.name]) != null ? (ref19 = ref18[method.name]) != null ? ref19.resultAccess : void 0 : void 0 : void 0 : void 0);
          method.result.$$validate = $$validateBuilder(method, 'result', method.result.$$access, methods != null ? (ref20 = methods.api) != null ? (ref21 = ref20[api.name]) != null ? (ref22 = ref21[method.name]) != null ? ref22.resultValidate : void 0 : void 0 : void 0 : void 0);
          method.result.$$editValidateBuilder = $$editValidateBuilderBuilder(method, 'result', method.arguments.$$access, methods != null ? (ref23 = methods.api) != null ? (ref24 = ref23[api.name]) != null ? (ref25 = ref24[method.name]) != null ? ref25.resultValidate : void 0 : void 0 : void 0 : void 0);
        }

        freeze(method);
      }

      freeze(api);
    }

    return freeze(config); // link =
  }; // ----------------------------


  module.exports = link;
}).call(void 0);